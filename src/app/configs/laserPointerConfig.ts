export const LASER_SIZE_MULTIPLIER = 1.3;
export const LASER_BEAM_OFFSET = 20;

// Rotation behavior
export const LASER_ROTATABLE = true;
// 0deg means horizontally facing right
export const LASER_INITIAL_ANGLE = 0;

// Rotation smoothing
export const ROTATION_LERP_SPEED = 0.18;
// Movement smoothing
export const MOVEMENT_LERP_SPEED = 0.25;
// Minimum movement threshold to trigger position update (prevents jitter)
export const MOVEMENT_EPSILON = 0.1;
